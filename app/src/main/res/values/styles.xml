<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!--<item name="colorPrimary">#06f</item>-->
        <!--<item name="colorPrimaryDark">#09f</item>-->
        <item name="colorAccent">#f0a</item>
    </style>

    <style name="AppThemeTranslucent" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!--向下兼容包中的主题，需要设置添加一个去掉"android:"的同类属性-->
    <!--此主题下，actionBar可覆盖在内容上-->
    <style name="AppTheme.Actionbar.Overlay" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!--动画的style-->
    <style name="Widget.GifMoviewView" parent="@android:style/Widget"></style>
</resources>
